- The bash.00 cost logging rate assigned to 'openai/gpt-oss-20b' is an assumption made for testing, and the model's actual free tier status via this proxy hasn't been verified on a pricing page. (If pricing changes, it may be silently inaccurate since it relies on the hardcoded 0.0 fallback logic for this specific string).
- Grading currently uses the same Groq-hosted model as quiz generation (`openai/gpt-oss-20b`), not a separate quality-tier model (e.g., OpenAI), pending OpenAI API key availability. This is a temporary substitution and may produce weaker/inconsistent open-ended grading results.
- Quiz generation can fall back on general knowledge if the retrieved project context is insufficient, meaning quiz questions are not strictly grounded in uploaded project materials.
- Concept extraction currently happens once per material during ingestion, with idempotency guards to prevent duplicate concepts *within the same material run*. However, cross-material concept de-duplication (e.g., merging 'machine learning' extracted from two different materials in the same Project into one concept) is not yet supported. Duplicate concepts across materials will result in separate concept rows.
